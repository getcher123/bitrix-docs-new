# Установка и настройка ОС

**Навигация**
- [← Оглавление курса](index.md)
- [← Предыдущий: 32638 — Конфигурация сайта](lesson_32638.md)
- [Следующий: 5321 — Установка пакетов →](lesson_5321.md)

Официальная страница урока: https://dev.1c-bitrix.ru/learning/course/index.php?COURSE_ID=135&LESSON_ID=5363

### Установка

Скачайте и установите дистрибутив Debian 11 с минимальным набором ПО, остальное будет установлено по сети во время настройки. В процессе установки выберите «сервер с минимальной настройкой», иначе получите десктопную версию.

Все последующие шаги описаны для сервера с минимальной настройкой.

В Debian 11 применяется система управления программными пакетами `apt/apt-get`. С ее помощью установите обновления до последней версии и перезапустите систему:

```
su -
apt update && apt upgrade
reboot
```

### Настройка портов

Обязательно откройте порты:

- 22 — ssh доступ,
- 80 / 443 — http / https web-сервер.

Остальные порты для ntlm, сервера мгновенных сообщений и xmpp-сервера нужно открыть, если только они используются. Можно выбрать произвольные порты, а можно те, что используются в [CentOS](https://dev.1c-bitrix.ru/learning/course/index.php?COURSE_ID=37&LESSON_ID=8811):

- 8890 / 8891 — http/https ntlm,
- 8893 / 8894 — http/https сервер мгновенных сообщений,
- 5222 / 5223 — http/https.

Чтобы открыть порты, можно установить пакет ufw:

```
su -
apt install ufw
```

Далее использовать команду:

```
ufw allow <номер_порта>
```
