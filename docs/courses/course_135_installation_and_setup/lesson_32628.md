# Конфигурация Apache

**Навигация**
- [← Оглавление курса](index.md)
- [← Предыдущий: 32626 — Конфигурация PHP](lesson_32626.md)
- [Следующий: 32630 — Конфигурация MariaDB →](lesson_32630.md)

Официальная страница урока: https://dev.1c-bitrix.ru/learning/course/index.php?COURSE_ID=135&LESSON_ID=32628

По умолчанию Apache настроен на дефолтный сайт в каталоге `/var/www/html`.

Основные действия для настройки конфигурации Apache:

- изменить каталог для сайта на `/var/www/html/bx-site`,
- изменить порт, который слушает сервис (так как в качестве внешнего сервиса будет использоваться Nginx),
- импортировать настройки для сайта из виртуальной машины.



Все конфигурационные файлы можно [скачать в архиве](/docs/chm_files/cfg.zip). Конфигурационные файлы для Apache расположены в папке `/cfg/httpd/`.

После того, как файлы конфигурации

			загружены на сервер

                    cd /opt

wget https://dev.1c-bitrix.ru/docs/chm_files/cfg.zip

unzip cfg.zip

		, выполните команду:

```
rsync -av /opt/cfg/httpd/ /etc/httpd/
```

В результате будет настроено два файла:

- `conf.d/default.conf` — в описании сайта заменен каталог на `var/www/html/bx-site`,
- `conf/httpd.conf` — изменено значение порта `Listen` на порт 8090.

Запустите Apache:

```
systemctl --now enable httpd
```
