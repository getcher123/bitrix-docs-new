# Информационные блоки в ядре D7

Список классов модуля **Информационные блоки** в ядре D7


| Класс, пространство имен | Описание |
| --- | --- |
| **BizprocType**Пространство содержит классы для работы со значениями полей инфоблоков пользовательских типов в бизнес-процессах. | Пространство содержит классы для работы со значениями полей инфоблоков пользовательских типов в бизнес-процессах. |
| **Component**Пространство содержит классы поддержки компонентов. | Пространство содержит классы поддержки компонентов. |
| **ElementTable**Класс для работы с таблицей элементов инфоблоков. | Класс для работы с таблицей элементов инфоблоков. |
| **Model**Пространство имен содержит классы для механизма единого управления свойствами. | Пространство имен содержит классы для механизма единого управления свойствами. |
| **Helpers**Пространство содержит вспомогательные классы. | Пространство содержит вспомогательные классы. |
| **IblockFieldTable**Класс для работы с таблицей полей инфоблоков. | Класс для работы с таблицей полей инфоблоков. |
| **IblockGroupTable**Класс для работы с таблицей прав доступа к инфоблокам. | Класс для работы с таблицей прав доступа к инфоблокам. |
| **IblockMessageTable**Класс для работы с таблицей подписей и заголовков объектов инфоблоков. | Класс для работы с таблицей подписей и заголовков объектов инфоблоков. |
| **IblockRssTable**Класс для работы с таблицей привязок полей для выгрузки в RSS. | Класс для работы с таблицей привязок полей для выгрузки в RSS. |
| **IblockSiteTable**Класс для работы с таблицей привязки инфоблоков к сайтам. | Класс для работы с таблицей привязки инфоблоков к сайтам. |
| **IblockTable**Класс для работы с таблицей информационных блоков. | Класс для работы с таблицей информационных блоков. |
| **InheritedProperty**Пространство содержит классы для работы с наследуемыми вычисляемыми свойствами. | Пространство содержит классы для работы с наследуемыми вычисляемыми свойствами. |
| **InheritedPropertyTable**Класс для работы с таблицей шаблонов вычисляемых наследуемых свойств. | Класс для работы с таблицей шаблонов вычисляемых наследуемых свойств. |
| **PropertyEnumerationTable**Класс для работы с таблицей вариантов значений свойств типа **Список**. | Класс для работы с таблицей вариантов значений свойств типа **Список**. |
| **PropertyIndex**Пространство содержит классы для работы с индексами инфоблоков. | Пространство содержит классы для работы с индексами инфоблоков. |
| **PropertyTable**Класс для работы с таблицей свойств инфоблоков. | Класс для работы с таблицей свойств инфоблоков. |
| **SectionElementTable**Класс для работы с таблицей элементов разделов. | Класс для работы с таблицей элементов разделов. |
| **SectionPropertyTable**Класс для работы с таблицей свойств разделов. | Класс для работы с таблицей свойств разделов. |
| **SectionTable**Класс для работы с таблицей свойств разделов. | Класс для работы с таблицей свойств разделов. |
| **SenderEventHandler**Технический класс для сообщения с модулем **Email-маркетинг**. | Технический класс для сообщения с модулем **Email-маркетинг**. |
| **SequenceTable**Класс для работы с таблицей счетчиков. | Класс для работы с таблицей счетчиков. |
| **Template**Пространство содержит классы для работы с шаблонами SEO свойств. | Пространство содержит классы для работы с шаблонами SEO свойств. |
| **TypeLanguageTable**Класс для работы с таблицей языковых параметров типов инфоблоков. | Класс для работы с таблицей языковых параметров типов инфоблоков. |
| **TypeTable**Класс для работы с таблицей типов информационных блоков. | Класс для работы с таблицей типов информационных блоков. |
