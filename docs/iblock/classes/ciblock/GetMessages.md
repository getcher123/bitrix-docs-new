# GetMessages


```
array
CIBlock::GetMessages(
	int ID,
	string type=""
);
```

Метод возвращает значения [дополнительных полей](../../fields.md#fiblocklang) инфоблока. Метод статический.


#### Параметры вызова


| Параметр | Описание | С версии |
| --- | --- | --- |
| ID | Код инфоблока. |  |
| type | Код типа инфоблоков. Параметр используется только тогда, когда инфоблок с указанным ID не найден или указан 0. В этом случае берутся значения дополнительных полей из этого типа. Если ID задан и такой инфоблок есть, то параметр type игнорируется. | 8.6.3 |


#### Возвращаемое значение

Массив значений [дополнительных полей](../../fields.md#fiblocklang) инфоблока.


#### Смотрите также


- [CIBlock::SetMessages](SetMessages.md)
- [Дополнительные поля](../../fields.md#fiblocklang)
